# Copy to .env and fill in. .env is gitignored.
# Any var in the Config table in README.md can go here.

# Password-gate the whole app. Leave blank to disable auth.
AUTH_PASSWORD=

# Optional — the launcher script will figure these out on macOS.
# BIND=100.64.0.1
# TMUX_BIN=/opt/homebrew/bin/tmux
